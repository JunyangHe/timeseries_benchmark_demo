# Data Directory

This folder will store benchmark data assets.

## Planned Contents

- `data/dataset.zip` (or equivalent archive used for reproducible runs)
- `data/raw/` extracted/downloaded raw source files
- `data/processed/` cleaned/intermediate files as needed

## Notes

- Add licensing/distribution notes before committing dataset artifacts.
- Keep data paths consistent with `notebooks/main.ipynb` and `src/` utilities.
